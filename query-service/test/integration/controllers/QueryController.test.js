
describe('QueryController', function () {

    describe('#find()', function () {
        it('should return all queries', function (done) {
            // request(sails.hooks.http.app)
            //     .post('/users/login')
            //     .send({ name: 'test', password: 'test' })
            //     .expect(302)
            //     .expect('location', '/mypage', done);
            done();
        });
    });

});